// ============================================================
// 笔记本 · 内置示例数据（只放内容，不含任何逻辑）
// 在下方数组中增删改条目即可，页面无需改动。
// 字段说明：
//   id    唯一标识（内置条目用 n 前缀，勿与用户 u 前缀重复）
//   title 笔记标题
//   date  记录日期（YYYY-MM-DD）
//   desc  笔记内容
//   user  内置条目固定 false（不可删除），用户笔记为 true
// ============================================================
window.NOTES_BUILTIN = [
  { id: 'n1', title: 'flex 与 grid 的取舍', date: '2026-09-21', desc: '一维排版用 flex（导航、按钮组），二维网格用 grid（卡片墙、整页布局）。两者可以嵌套混用，不必二选一。', user: false },
  { id: 'n2', title: 'localStorage 使用备忘', date: '2026-09-20', desc: '只能存字符串，对象要 JSON.stringify / JSON.parse；读写都要 try-catch 兜底，隐私模式下会抛错。', user: false },
];
