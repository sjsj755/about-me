# 依赖
node_modules/

# 测试产物（tests/ 为关键路径，必须入库；这里只忽略运行产物）
playwright-report/
test-results/

# 视觉基线快照（Phase B CSS 拆分的一次性验证产物，不入库；基线丢失时重跑 visual.spec.js 重建）
tests/visual.spec.js-snapshots/

# 本地一次性截图对比目录
_font-compare/

# IDE / 工具配置
.trae/
.vscode/

# 部署配置（含域名与本地路径，不入库）
deploy/

# 隐私文件（个人简历及渲染产物，不入库）
*简历*
_resume_p*.png
_render_check.pdf

# 系统文件
.DS_Store
Thumbs.db
desktop.ini

# 日志与临时文件
*.log
npm-debug.log*
